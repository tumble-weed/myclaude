---
description: too wide — last message had lines/tables wider than ~40 cols; rewrap to fit
---
look at your last message. did any prose lines or tables run wider than ~40 columns (a mobile / vim-40 view)? if yes, rewrap: shorten lines, and break wide tables into stacked lists or fewer columns. exception: code blocks are exempt — leave them at whatever width the language needs. if it already fit, say so and stop.
